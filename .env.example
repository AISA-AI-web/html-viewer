# Public Supabase config — safe to expose (all access is gated by Row-Level
# Security). Copy to `.env` for local dev, or set these as GitHub Actions
# repository *Variables* (Settings → Secrets and variables → Actions →
# Variables) so the deployed build uses them. If unset, the committed defaults
# in src/lib/config.js are used.
VITE_SUPABASE_URL=https://ojifqcnsvbvkvvpvwmovr.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_87Sk_zzm6QLFeDfi3E_woQ_Kw3rCzyx
